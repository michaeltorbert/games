Original prompt: Enrich the released Place by Place game without touching other games: preserve the existing scheduler and parent stage choice; correct the visible base-ten geometry; add four-choice misconception-aware number-word questions with word-form remediation and durable review scheduling; and teach zero patterns in fixed columns before unlocking genuinely position-free transfer layouts after distinct first-try evidence in both directions.

## Product and interaction thesis

- Keep the released app's calm, rounded, child-friendly visual language and its short adaptive sessions.
- Make the mathematics physically truthful: within each rendered question, one visible unit `u` defines a cube (`u × u`), a rod (`u × 10u`), and a flat (`10u × 10u`). Responsive scaling changes `u` for the whole representation, never one block type independently.
- Add variety through recognition and transfer, not speed pressure: number words use four plausible choices and zero-pattern transfer starts with visible H–T–O columns before moving to unlabelled, shuffled blocks.
- A wrong choice never earns first-try/mastery credit. Hints move from strategy to structure to an explicit answer, and any miss schedules later review.
- Preserve the existing eight-question adaptive promotion/demotion window. Do not add a five-answer fast path or a Fluent Start default.

## Work log

- 2026-07-18: Read the complete current single-file game, repository instructions, and the develop-web-game workflow. Confirmed the branch starts clean at `origin/main`; no prior place-value progress file existed.
- 2026-07-18: Implemented shared-unit geometry. Each question chooses one responsive unit and renders cube `u × u`, rod `u × 10u`, and flat `10u × 10u`; high-density questions scale the entire system together. Rounded edges, grid lines, restrained animation, and the existing palette remain.
- 2026-07-18: Added `number-words` as a focus format and as part of default Mixed Practice. It covers 0–999, generates four deterministic/randomized unique choices from explicit misconception families, disables missed choices, disqualifies first-try credit after any miss, uses word-only remediation, and persists exact/category review tickets in the same adaptive queues.
- 2026-07-18: Added durable evidence for H0O, HT0, and H00. Each category unlocks only after a first-try fixed blocks-to-number answer and a first-try number-to-places answer on a different value. Unlocking schedules a free transfer prompt; its first miss stays shuffled with a type/value cue, its second miss restores H–T–O columns, and a miss schedules another free transfer ticket.
- 2026-07-18: Preserved the adaptive scheduler unchanged (7 first-try answers in an 8-question ordinary window promote; 5 or fewer demote). Added an explicit regression proving five perfect answers do not promote.
- 2026-07-18: Expanded `render_game_to_text` with format, layout, transfer category/evidence, restored-column state, visible block counts, and visible word choices. Extended the existing deterministic browser self-test suite, including geometry ratios, word choices/remediation/review, distinct bidirectional transfer gates, free-layout remediation/retest, and scheduler preservation.
- 2026-07-18 (consensus editor pass, A–G): Corrected phone (`<=720px`) block densities to exactly representable unit/ten pairs (`5/50`, `4.5/45`, `4/40`) so a flat is ten rods and a rod is ten cubes in rendered geometry. Made `chooseQuestion` filter the entire exact/category/transfer queues by the active focus before selecting a due ticket: Mixed accepts all; a focused format accepts exact/category tickets only when the stored format is null (legacy) or exactly matches; transfer prompts are eligible only in Mixed or Blocks; incompatible tickets are skipped, not mutated. Replaced the first number-word hint with value-sensitive, word-only guidance for exactly five domains (zero; 1–19; 20–99; exact hundreds; other three-digit), keeping the later `wordParts` remediation. Hardened `normalizeTransferEvidence` to retain only genuinely digit-matching values per category and to derive `unlocked` solely from a valid different-value pair (ignoring the stored boolean); persistence now prunes transfer tickets for locked categories, coerces a gateless restored free question to fixed, discards a stale active transfer question, and revalidates `activeTicket`. Fixed distractor ordering to keep the first three pedagogically prioritized unique word distractors before shuffling only display positions (305 always retains 35 and 350; every 0–999 keeps exactly four unique choices). Made `advanceTime` only advance/return the deterministic test clock with no `render()`. Bumped Place by Place to `1.1.0` in `games.js` and `version.json` (Football `1.25.0` untouched).
- 2026-07-18: Completed visual QA of fixed, number-word, free-first-miss, and restored-column states at desktop (>=900px), tablet (721–899px), and phone (<=720px, incl. <=440px) widths. Confirmed the shared-unit base-ten geometry holds at every density and that the phone pairs render a flat as exactly ten rods and a rod as exactly ten cubes. To avoid the generic web-game client's per-frame re-render restarting entrance animations mid-capture, `advanceTime` no longer renders; screenshots used direct state forcing.
- 2026-07-18: Hardened restored-review identity across exact, category, and transfer tickets while preserving completed first-try review screens after their tickets are consumed. Reconciled malformed saved word-choice history so prior wrong choices cannot regain first-try credit, corrected the zero/teen/exact-ten hint language, and replaced wrapped digit-change distractors with adjacent non-wrapping changes.
- 2026-07-18: Added deterministic embedded regressions for phone geometry, focused-queue compatibility (including legacy null format and whole-queue filtering), all five first-hint buckets, semantic review-ticket restoration, completed-review reloads, pending recovery, malformed word-choice history, prioritized and adjacent distractors, and `advanceTime` DOM node identity. The browser self-test suite now runs 98 passing checks; `node --check` and `git diff --check` pass with no errors.
- 2026-07-18 (Place by Place gate revision): Removed Number to Place Values from settings, Mixed, forms, prompts, handlers, rendering, persistence restoration, test helpers, and transfer evidence. Mixed now contains only Blocks to Number (52%) and Number Words (48%). Defensive migration maps legacy focus settings to Mixed; drops legacy active questions, attempts, and review tickets; ignores legacy number-to-places evidence; and preserves valid fixed Blocks to Number values.
- 2026-07-18: Replaced bidirectional transfer gates with one durable invariant per H0O, HT0, and H00 family: two distinct unaided first-try fixed Blocks to Number values unlock the permanent free-layout try. Added the one-shot opportunity scheduler at aggregate completion +2, existing remediation priority, current-stage/focus eligibility, longest-waiting selection, recent-value avoidance, immediate consumption, no re-arm after a miss, and a two-additional-question global service gap. Reload normalization repairs stale tickets, free questions, opportunity timing, cooldowns, and completed legacy-session boundaries without changing the adaptive scheduler.
- 2026-07-18: Expanded the embedded deterministic suite to 139 passing checks (0 failures), including all evidence exclusions, all three families, natural cancellation, eligibility and priority, success/miss/crash recovery, spacing, migration, free remediation/retest, text-state diagnostics, and adaptive preservation. Inline JavaScript passes `node --check`; `git diff --check` passes. The develop-web-game client completed on the final file, and manual Playwright QA inspected settings, fixed practice, unlocked free layout, first free miss, and restored-column remediation at desktop, tablet, and phone sizes (15 screenshots total) with no page or console errors; all artifacts stayed under `/tmp`.
- 2026-07-19 (four-seat packet implementation): Split fixed-evidence unlock from durable initial-transfer completion with an independent `probeCleared` marker per H0O, HT0, and H00 family. Live unlock now guarantees one delayed initial transfer ticket; ordinary/retention free layout remains fixed until a clean first-try transfer probe/retest clears that marker and its ticket. Transfer misses retain the free cue, restored H-T-O, and later retest; transfer work never enters the adaptive window. Successful post-probe organic free work creates no retest, while a helped or revealed organic free completion queues one same-family transfer retest without resetting `probeCleared`. Persistence conservatively re-arms exactly one missing initial probe and preserves pre/post-probe readiness.
- 2026-07-19: Made category and transfer scheduling exhaust every due ticket in stable due/order, postponing each chosen-stage incompatibility once before allowing gates or ordinary work. Chosen-stage category candidates now come directly from that stage domain, restoring Stage 7 H00, H0O, HT0, multiple-hundreds, all-nonzero, and number-word reviews. The embedded Chromium suite passes 159/159 with no page or console errors; inline JavaScript `node --check` and `git diff --check` pass. The standard develop-web-game client completed, and targeted Playwright QA inspected six states—fixed pre-probe, free transfer, first-miss cue, restored H-T-O, clean organic-free readiness, and settings—with artifacts under `/tmp/place-by-place-f1f3-a80a-20260719`.
- 2026-07-19 (round-2 organic-free retest correction): Post-probe ordinary and retention free-layout helped/revealed completions now create or reschedule exactly one same-family transfer ticket while retaining durable `probeCleared`; successful organic work still creates none. A pending ticket suppresses only newly selected organic free work, so the just-completed corrected free presentation survives normalization/reload without adopting that ticket as `activeTicket`; the later ticket-backed free retest reschedules on a miss and restores readiness when completed cleanly. Added deterministic coverage for ordinary/retention/revealed outcomes, repeated normalization, pending-write recovery, fixed interim selection, ticket identity, retest miss/consume, and organic first-try success. The full embedded Chromium suite passes 169/169 with zero failures and zero page/console errors; inline JavaScript `node --check` and `git diff --check` pass. The standard develop-web-game client completed, and targeted browser QA exercised the full miss → correction → real reload → fixed interim → free retest → clean consume lifecycle plus settings, with 11/11 focused assertions, matching `render_game_to_text`, inspected screenshots, and no console/page errors; artifacts are under `/tmp/place-by-place-round2-qa` and `/tmp/place-by-place-round2-client`.
