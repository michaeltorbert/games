{
  "name": "games-verifier",
  "private": true,
  "description": "Repo-local UI verification tooling for games. No runtime deps; no bundler; no app build step.",
  "scripts": {
    "test:football": "playwright test tests/football-call-layout.spec.mjs",
    "test:football:release": "playwright test tests/football-call-layout.spec.mjs tests/football-audio.spec.mjs tests/football-particles.spec.mjs tests/football-copy-contract.spec.mjs tests/football-fireworks.spec.mjs tests/football-overlay-accessibility.spec.mjs tests/football-release-matrix.spec.mjs"
  },
  "devDependencies": {
    "@playwright/test": "1.56.1"
  }
}
